% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_evolution.R
\name{prepare_phylo_data}
\alias{prepare_phylo_data}
\title{Prepare phylogenetic data for evolutionary models}
\usage{
prepare_phylo_data(
  phy,
  species,
  niche_params,
  occ_data,
  env_m_data,
  mu_anc_prior = c(0, 0),
  mu_anc_sigma = c(5, 5),
  rate_mu_scale = 1,
  rate_log_sigma_scale = 0.5,
  z_rho_anc_prior = 0,
  z_rho_anc_sigma = 1,
  rate_rho_scale = 0.5,
  alpha_mu_scale = c(1, 1),
  sigma_sq_mu_scale = c(1, 1),
  alpha_log_sigma_scale = c(1, 1),
  sigma_sq_log_sigma_scale = c(1, 1),
  alpha_rho_scale = 1,
  sigma_sq_rho_scale = 1,
  grain_size = 1
)
}
\arguments{
\item{phy}{An object of class \code{phylo} from the \pkg{ape} package,
representing the phylogenetic tree of the species.}

\item{species}{Character vector of species names, in the same order as the
rows of \code{niche_params}.}

\item{niche_params}{A matrix or data.frame with columns:
\code{mu_1}, \code{mu_2} (centroid dimensions),
\code{sigma_1}, \code{sigma_2} (tolerances),
\code{rho} (correlation), one row per species.}

\item{occ_data}{A list of matrices, one per species, with environmental
values at occurrence points.}

\item{env_m_data}{A list of matrices, one per species, with environmental
values at background points.}

\item{mu_anc_prior}{Numeric vector of length 2, prior mean for ancestral
centroid.}

\item{mu_anc_sigma}{Numeric vector of length 2, prior SD for ancestral
centroid.}

\item{rate_mu_scale}{Numeric, scale for half-normal prior on BM rate of
centroid.}

\item{rate_log_sigma_scale}{Numeric, scale for half-normal prior on BM rate
of log tolerances.}

\item{z_rho_anc_prior}{Numeric, prior mean for ancestral z_rho (atanh(rho)).}

\item{z_rho_anc_sigma}{Numeric, prior SD for ancestral z_rho.}

\item{rate_rho_scale}{Numeric, scale for half-normal prior on BM rate of
z_rho.}

\item{alpha_mu_scale}{Numeric vector of length 2, scale for prior on OU
alpha (centroid).}

\item{sigma_sq_mu_scale}{Numeric vector of length 2, scale for prior on OU
sigma^2 (centroid).}

\item{alpha_log_sigma_scale}{Numeric vector of length 2, scale for prior on
OU alpha (log tolerances).}

\item{sigma_sq_log_sigma_scale}{Numeric vector of length 2, scale for prior
on OU sigma^2 (log tolerances).}

\item{alpha_rho_scale}{Numeric, scale for prior on OU alpha (rho).}

\item{sigma_sq_rho_scale}{Numeric, scale for prior on OU sigma^2 (rho).}

\item{grain_size}{Integer, grainsize for \code{reduce_sum} parallelisation.}
}
\value{
A named list suitable for passing to \code{fit_evolution()} as the
  \code{stan_data} argument.
}
\description{
Converts a phylogeny, species niche estimates, and environmental data into
the list format required by the Stan evolutionary models.
}
